{
  "$defs": {
    "AccelerationEvidence": {
      "description": "Explicit acceleration support evidence without marketing-name inference.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/AccelerationKind",
          "description": "Acceleration API being described."
        },
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "supported": {
          "description": "Observed support when reliably probed.",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "kind",
        "metadata"
      ],
      "type": "object"
    },
    "AccelerationKind": {
      "description": "Acceleration API relevant to future local runtime readiness.",
      "oneOf": [
        {
          "const": "direct_ml",
          "description": "Microsoft DirectML acceleration.",
          "type": "string"
        },
        {
          "const": "cuda",
          "description": "NVIDIA CUDA acceleration.",
          "type": "string"
        },
        {
          "const": "rocm",
          "description": "AMD ROCm acceleration.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The acceleration API is unknown or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "ApplicationInfo": {
      "description": "Application and boundary-version information exposed by the platform core.",
      "properties": {
        "application_id": {
          "description": "Stable application identity used by desktop packaging.",
          "type": "string"
        },
        "name": {
          "description": "User-facing product name.",
          "type": "string"
        },
        "protocol_version": {
          "description": "Transport contract version expected by compatible clients.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "schema_version": {
          "description": "Serialized contract schema version.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "version": {
          "description": "Semantic version of the running platform core.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "application_id",
        "version",
        "protocol_version",
        "schema_version"
      ],
      "type": "object"
    },
    "ArchitectureEvidence": {
      "description": "Machine architecture plus its collection metadata.",
      "properties": {
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "value": {
          "anyOf": [
            {
              "$ref": "#/$defs/MachineArchitecture"
            },
            {
              "type": "null"
            }
          ],
          "description": "Normalized architecture when available."
        }
      },
      "required": [
        "metadata"
      ],
      "type": "object"
    },
    "BootstrapReady": {
      "description": "Public bootstrap result returned through stdout after loopback binding.",
      "properties": {
        "instance_id": {
          "$ref": "#/$defs/InstanceId",
          "description": "Opaque identifier for the running sidecar instance."
        },
        "port": {
          "description": "Dynamically assigned loopback TCP port.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "protocol_max": {
          "description": "Newest protocol version supported by the sidecar.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "protocol_min": {
          "description": "Oldest protocol version supported by the sidecar.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "schema_version": {
          "description": "Schema generation used by this bootstrap response.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema_version",
        "port",
        "instance_id",
        "protocol_min",
        "protocol_max"
      ],
      "type": "object"
    },
    "BootstrapRequest": {
      "description": "One-line secret bootstrap payload written through the inherited stdin pipe.",
      "properties": {
        "bearer_token": {
          "description": "Hex-encoded 256-bit per-launch bearer token.",
          "type": "string"
        },
        "protocol_max": {
          "description": "Newest protocol version accepted by the launching desktop.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "protocol_min": {
          "description": "Oldest protocol version accepted by the launching desktop.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "supervision_nonce": {
          "$ref": "#/$defs/SupervisionNonce",
          "description": "Opaque nonce unique to this supervision session."
        },
        "supervisor_process_id": {
          "description": "Process identifier of the supervising desktop process.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "bearer_token",
        "protocol_min",
        "protocol_max",
        "supervisor_process_id",
        "supervision_nonce"
      ],
      "type": "object"
    },
    "CancelOperationRequest": {
      "description": "Authenticated explicit cancellation request.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared with the operation being cancelled."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this cancellation request."
        }
      },
      "required": [
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "CancelOperationResponse": {
      "description": "Result of accepting or observing an explicit cancellation request.",
      "properties": {
        "accepted": {
          "description": "Whether the host accepted cancellation for a non-terminal operation.",
          "type": "boolean"
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier copied from the cancellation request."
        },
        "operation_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Operation targeted by the cancellation request."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier copied from the cancellation request."
        }
      },
      "required": [
        "operation_id",
        "accepted",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "ClientHello": {
      "description": "First authenticated request sent by the Flutter desktop.",
      "properties": {
        "client_name": {
          "description": "Stable client name, such as the GixGiz desktop shell.",
          "type": "string"
        },
        "client_version": {
          "description": "Semantic version of the desktop client.",
          "type": "string"
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by the handshake response and any safe failure."
        },
        "protocol_max": {
          "description": "Newest protocol version accepted by the client.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "protocol_min": {
          "description": "Oldest protocol version accepted by the client.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this handshake request."
        },
        "requested_capabilities": {
          "description": "Optional capabilities requested for this launch session.",
          "items": {
            "$ref": "#/$defs/TransportCapability"
          },
          "type": "array"
        }
      },
      "required": [
        "client_name",
        "client_version",
        "protocol_min",
        "protocol_max",
        "requested_capabilities",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "CoreHello": {
      "description": "Successful protocol and capability negotiation returned by the core.",
      "properties": {
        "application": {
          "$ref": "#/$defs/ApplicationInfo",
          "description": "Identity and versions compiled into the running core."
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Correlation identifier copied from the client hello."
        },
        "instance_id": {
          "$ref": "#/$defs/InstanceId",
          "description": "Opaque identifier for this sidecar process instance."
        },
        "readiness": {
          "$ref": "#/$defs/ReadinessReport",
          "description": "Authoritative current core readiness."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Request identifier copied from the client hello."
        },
        "selected_protocol": {
          "description": "Protocol version selected from the overlapping supported ranges.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "supported_capabilities": {
          "description": "Capabilities supported by this internal core build.",
          "items": {
            "$ref": "#/$defs/TransportCapability"
          },
          "type": "array"
        }
      },
      "required": [
        "application",
        "selected_protocol",
        "supported_capabilities",
        "readiness",
        "instance_id",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "CorrelationId": {
      "description": "Identifier shared by events and failures from one logical operation.",
      "format": "uuid",
      "type": "string"
    },
    "CpuEvidence": {
      "description": "CPU identity and topology evidence.",
      "properties": {
        "logical_core_count": {
          "$ref": "#/$defs/U32Evidence",
          "description": "Sum of logical processor counts across reported processor packages."
        },
        "name": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Bounded CPU product identity when consistently reported."
        },
        "physical_core_count": {
          "$ref": "#/$defs/U32Evidence",
          "description": "Sum of physical core counts across reported processor packages."
        },
        "vendor": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Bounded CPU vendor when consistently reported."
        }
      },
      "required": [
        "name",
        "vendor",
        "physical_core_count",
        "logical_core_count"
      ],
      "type": "object"
    },
    "ErrorCategory": {
      "description": "Stable failure taxonomy used across GixGiz process boundaries.",
      "oneOf": [
        {
          "const": "invalid_input",
          "description": "Input failed validation.",
          "type": "string"
        },
        {
          "const": "permission_denied",
          "description": "The caller lacks permission for the requested operation.",
          "type": "string"
        },
        {
          "const": "not_supported",
          "description": "The requested capability is not supported.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "A required resource or service is currently unavailable.",
          "type": "string"
        },
        {
          "const": "conflict",
          "description": "Current state conflicts with the requested operation.",
          "type": "string"
        },
        {
          "const": "resource_exhausted",
          "description": "A bounded resource is exhausted.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "The operation was cancelled.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "The operation exceeded its deadline.",
          "type": "string"
        },
        {
          "const": "integrity_failure",
          "description": "Integrity verification failed.",
          "type": "string"
        },
        {
          "const": "incompatible_version",
          "description": "A component or protocol version is incompatible.",
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "The operation completed with reduced capability.",
          "type": "string"
        },
        {
          "const": "internal",
          "description": "An internal failure has no safer specific category.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized category.",
          "type": "string"
        }
      ]
    },
    "EvidenceAvailability": {
      "description": "Availability of one observed hardware field or collection.",
      "oneOf": [
        {
          "const": "available",
          "description": "A bounded value was collected from the stated source.",
          "type": "string"
        },
        {
          "const": "permission_denied",
          "description": "The current user could not access the evidence.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "Collection did not finish within its deadline.",
          "type": "string"
        },
        {
          "const": "unsupported",
          "description": "The provider does not support this evidence.",
          "type": "string"
        },
        {
          "const": "not_present",
          "description": "The device or metric is not present.",
          "type": "string"
        },
        {
          "const": "not_reliable",
          "description": "A value exists but is not reliable enough to report as available.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The source could not determine availability or a newer peer supplied an unrecognized value.",
          "type": "string"
        }
      ]
    },
    "EvidenceConfidence": {
      "description": "Confidence attached to one normalized evidence value.",
      "oneOf": [
        {
          "const": "high",
          "description": "The source directly and reliably reported the value.",
          "type": "string"
        },
        {
          "const": "medium",
          "description": "The value is useful but has documented source limitations.",
          "type": "string"
        },
        {
          "const": "low",
          "description": "The value is weak evidence and must not drive a decision alone.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "Confidence is unavailable or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "EvidenceMetadata": {
      "description": "Source, availability, confidence, and safe reason for one evidence value.",
      "properties": {
        "availability": {
          "$ref": "#/$defs/EvidenceAvailability",
          "description": "Explicit availability state."
        },
        "confidence": {
          "$ref": "#/$defs/EvidenceConfidence",
          "description": "Confidence in the normalized value."
        },
        "reason": {
          "description": "Safe bounded explanation containing no raw provider output.",
          "type": [
            "string",
            "null"
          ]
        },
        "reason_code": {
          "anyOf": [
            {
              "$ref": "#/$defs/UnknownReasonCode"
            },
            {
              "type": "null"
            }
          ],
          "description": "Stable reason category when the value is unavailable."
        },
        "source": {
          "$ref": "#/$defs/EvidenceSource",
          "description": "Provider-neutral source category."
        }
      },
      "required": [
        "source",
        "availability",
        "confidence"
      ],
      "type": "object"
    },
    "EvidenceSource": {
      "description": "Provider-neutral source category for one hardware observation.",
      "oneOf": [
        {
          "const": "windows_cim",
          "description": "Windows Common Information Model data accessed without elevation.",
          "type": "string"
        },
        {
          "const": "windows_environment",
          "description": "A bounded Windows process environment value.",
          "type": "string"
        },
        {
          "const": "derived",
          "description": "A normalized value derived only from other reported evidence.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The source is unavailable or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "GpuCollectionEvidence": {
      "description": "Bounded GPU collection and collection-level availability.",
      "properties": {
        "devices": {
          "description": "Display adapters reported by the provider, in stable provider order.",
          "items": {
            "$ref": "#/$defs/GpuEvidence"
          },
          "type": "array"
        },
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Availability of the collection itself."
        }
      },
      "required": [
        "devices",
        "metadata"
      ],
      "type": "object"
    },
    "GpuEvidence": {
      "description": "Evidence for one display adapter without stable device identifiers.",
      "properties": {
        "dedicated_memory_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Dedicated video memory in bytes only when reliably obtainable."
        },
        "name": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Bounded display-adapter name."
        },
        "shared_memory_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Shared video memory in bytes only when reliably obtainable."
        },
        "vendor": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Bounded display-adapter vendor."
        }
      },
      "required": [
        "name",
        "vendor",
        "dedicated_memory_bytes",
        "shared_memory_bytes"
      ],
      "type": "object"
    },
    "HardwareScanEvent": {
      "description": "Ordered event carrying progress or the final machine profile.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by every event for the scan."
        },
        "error": {
          "anyOf": [
            {
              "$ref": "#/$defs/SafeErrorPayload"
            },
            {
              "type": "null"
            }
          ],
          "description": "Safe typed failure present only for timed-out or failed terminal scans."
        },
        "kind": {
          "$ref": "#/$defs/HardwareScanEventKind",
          "description": "Stable event classification."
        },
        "message": {
          "description": "Optional safe provider-neutral status explanation.",
          "type": [
            "string",
            "null"
          ]
        },
        "operation_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Operation that owns this event stream."
        },
        "profile": {
          "anyOf": [
            {
              "$ref": "#/$defs/MachineProfile"
            },
            {
              "type": "null"
            }
          ],
          "description": "Final profile present only for successful complete or partial scans."
        },
        "schema_version": {
          "description": "Contract schema generation for this event.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "sequence": {
          "description": "Monotonically increasing sequence number starting at one.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "terminal_state": {
          "anyOf": [
            {
              "$ref": "#/$defs/HardwareScanTerminalState"
            },
            {
              "type": "null"
            }
          ],
          "description": "Terminal state present only on the final event."
        },
        "timestamp_unix_ms": {
          "description": "UTC Unix timestamp in milliseconds captured by the host.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema_version",
        "operation_id",
        "correlation_id",
        "sequence",
        "kind",
        "timestamp_unix_ms"
      ],
      "type": "object"
    },
    "HardwareScanEventKind": {
      "description": "Stable kind emitted by one hardware scan operation.",
      "oneOf": [
        {
          "const": "started",
          "description": "The operation was accepted and began.",
          "type": "string"
        },
        {
          "const": "collecting",
          "description": "The provider is collecting bounded hardware evidence.",
          "type": "string"
        },
        {
          "const": "completed",
          "description": "A complete or partial profile is available.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "Explicit cancellation was observed.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "Collection exceeded its bounded deadline.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "Collection failed without a usable profile.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized event kind.",
          "type": "string"
        }
      ]
    },
    "HardwareScanStartRequest": {
      "description": "Authenticated request to start one hardware evidence scan.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by the scan, events, and safe failures."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this start request."
        }
      },
      "required": [
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "HardwareScanStartResponse": {
      "description": "Accepted hardware scan operation.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier copied from the start request."
        },
        "operation_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Opaque identifier used for event subscription and cancellation."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier copied from the start request."
        }
      },
      "required": [
        "operation_id",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "HardwareScanTerminalState": {
      "description": "Terminal result for one hardware scan stream.",
      "oneOf": [
        {
          "const": "completed",
          "description": "All requested evidence was collected reliably.",
          "type": "string"
        },
        {
          "const": "partial",
          "description": "A usable profile contains explicitly unavailable evidence.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "Explicit cancellation was observed.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "Collection exceeded its bounded deadline.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "Collection failed without a usable profile.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized terminal state.",
          "type": "string"
        }
      ]
    },
    "HealthRequest": {
      "description": "Authenticated request for current platform status.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by the response and any safe failure."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this request."
        }
      },
      "required": [
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "HealthResponse": {
      "description": "Current authoritative core status with boundary identifiers.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier copied from the request."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier copied from the request."
        },
        "status": {
          "$ref": "#/$defs/PlatformStatus",
          "description": "Version and readiness reported by the running platform core."
        }
      },
      "required": [
        "status",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "InstanceId": {
      "description": "Opaque identifier for one running core instance.",
      "format": "uuid",
      "type": "string"
    },
    "MachineArchitecture": {
      "description": "Normalized machine architecture.",
      "oneOf": [
        {
          "const": "x86_64",
          "description": "64-bit x86 architecture.",
          "type": "string"
        },
        {
          "const": "arm64",
          "description": "64-bit Arm architecture.",
          "type": "string"
        },
        {
          "const": "x86",
          "description": "32-bit x86 architecture.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The architecture is unknown or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "MachineProfile": {
      "description": "Versioned provider-neutral evidence profile produced by one hardware scan.",
      "properties": {
        "acceleration": {
          "description": "Explicit acceleration evidence without capability decisions.",
          "items": {
            "$ref": "#/$defs/AccelerationEvidence"
          },
          "type": "array"
        },
        "completeness": {
          "$ref": "#/$defs/MachineProfileCompleteness",
          "description": "Whether every requested field was collected reliably."
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Correlation identifier shared with the initiating request."
        },
        "cpu": {
          "$ref": "#/$defs/CpuEvidence",
          "description": "CPU evidence."
        },
        "gpus": {
          "$ref": "#/$defs/GpuCollectionEvidence",
          "description": "Display-adapter evidence."
        },
        "operating_system": {
          "$ref": "#/$defs/OperatingSystemEvidence",
          "description": "Operating-system evidence."
        },
        "physical_memory": {
          "$ref": "#/$defs/PhysicalMemoryEvidence",
          "description": "Physical-memory evidence."
        },
        "scan_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Opaque scan operation identifier."
        },
        "scanned_at_unix_ms": {
          "description": "UTC Unix timestamp in milliseconds captured at scan completion.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "schema_version": {
          "description": "Machine-profile schema generation.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "storage": {
          "$ref": "#/$defs/StorageEvidence",
          "description": "Evidence for the selected storage location."
        }
      },
      "required": [
        "schema_version",
        "scan_id",
        "correlation_id",
        "scanned_at_unix_ms",
        "completeness",
        "operating_system",
        "cpu",
        "physical_memory",
        "gpus",
        "acceleration",
        "storage"
      ],
      "type": "object"
    },
    "MachineProfileCompleteness": {
      "description": "Whether all requested hardware evidence was collected reliably.",
      "oneOf": [
        {
          "const": "complete",
          "description": "Every requested field has reliable available evidence.",
          "type": "string"
        },
        {
          "const": "partial",
          "description": "The profile is usable evidence, but at least one field is unavailable.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized completeness value.",
          "type": "string"
        }
      ]
    },
    "OperatingSystemEvidence": {
      "description": "Operating-system evidence safe to cross the desktop boundary.",
      "properties": {
        "architecture": {
          "$ref": "#/$defs/ArchitectureEvidence",
          "description": "Normalized operating-system architecture."
        },
        "build": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Product build reported by the operating system."
        },
        "name": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Product name reported by the operating system."
        },
        "version": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Product version reported by the operating system."
        }
      },
      "required": [
        "name",
        "version",
        "build",
        "architecture"
      ],
      "type": "object"
    },
    "OperationId": {
      "description": "Opaque identifier for one core operation.",
      "format": "uuid",
      "type": "string"
    },
    "PhysicalMemoryEvidence": {
      "description": "Physical-memory evidence, excluding virtual-memory totals.",
      "properties": {
        "available_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Currently available physical memory normalized to bytes."
        },
        "total_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Total visible physical memory normalized to bytes."
        }
      },
      "required": [
        "total_bytes",
        "available_bytes"
      ],
      "type": "object"
    },
    "PlatformStatus": {
      "description": "Combined version and readiness response produced by the platform core.",
      "properties": {
        "application": {
          "$ref": "#/$defs/ApplicationInfo",
          "description": "Identity and version information for the responding core."
        },
        "readiness": {
          "$ref": "#/$defs/ReadinessReport",
          "description": "Current readiness plus its supporting service evidence."
        }
      },
      "required": [
        "application",
        "readiness"
      ],
      "type": "object"
    },
    "ReadinessReport": {
      "description": "Overall readiness plus the complete evidence used to calculate it.",
      "properties": {
        "services": {
          "description": "Complete service evidence used for the calculation.",
          "items": {
            "$ref": "#/$defs/ServiceHealth"
          },
          "type": "array"
        },
        "status": {
          "$ref": "#/$defs/ReadinessStatus",
          "description": "Overall readiness calculated from all service evidence."
        },
        "summary": {
          "description": "Safe plain-language explanation of the calculated status.",
          "type": "string"
        }
      },
      "required": [
        "status",
        "summary",
        "services"
      ],
      "type": "object"
    },
    "ReadinessStatus": {
      "description": "Overall readiness calculated from explicit service evidence.",
      "oneOf": [
        {
          "const": "ready",
          "description": "Every mandatory service is healthy and no service is impaired.",
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "The core is usable, but at least one capability is impaired.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "A mandatory service is not currently available.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "A mandatory service reported an explicit failure.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized readiness value.",
          "type": "string"
        }
      ]
    },
    "RecoveryAction": {
      "description": "Recommended safe next action for a boundary failure.",
      "oneOf": [
        {
          "const": "retry",
          "description": "Retry the operation without changing configuration.",
          "type": "string"
        },
        {
          "const": "restart",
          "description": "Restart the affected GixGiz component.",
          "type": "string"
        },
        {
          "const": "check_prerequisites",
          "description": "Review and correct platform prerequisites.",
          "type": "string"
        },
        {
          "const": "contact_support",
          "description": "Escalate using sanitized diagnostics.",
          "type": "string"
        },
        {
          "const": "no_action",
          "description": "No recovery action is necessary.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized recovery action.",
          "type": "string"
        }
      ]
    },
    "RecoveryGuidance": {
      "description": "Plain-language recovery information safe to show to a user.",
      "properties": {
        "action": {
          "$ref": "#/$defs/RecoveryAction",
          "description": "Stable action that a client may present or interpret."
        },
        "message": {
          "description": "Safe plain-language explanation of the next step.",
          "type": "string"
        }
      },
      "required": [
        "action",
        "message"
      ],
      "type": "object"
    },
    "RequestId": {
      "description": "Identifier for one request within a wider correlated operation.",
      "format": "uuid",
      "type": "string"
    },
    "SafeErrorPayload": {
      "description": "Error payload that excludes raw causes, private input, and provider output.",
      "properties": {
        "category": {
          "$ref": "#/$defs/ErrorCategory",
          "description": "Stable broad classification of the failure."
        },
        "code": {
          "description": "Stable machine-readable error code.",
          "type": "string"
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by related operations and events."
        },
        "message": {
          "description": "Safe user-facing error message.",
          "type": "string"
        },
        "recovery": {
          "$ref": "#/$defs/RecoveryGuidance",
          "description": "Recommended recovery action and explanation."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to the failed request."
        }
      },
      "required": [
        "category",
        "code",
        "message",
        "recovery",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "ServiceHealth": {
      "description": "Safe, provider-neutral health record for one platform service.",
      "properties": {
        "display_name": {
          "description": "Safe user-facing service name.",
          "type": "string"
        },
        "message": {
          "description": "Optional safe explanation containing no raw provider output.",
          "type": [
            "string",
            "null"
          ]
        },
        "requirement": {
          "$ref": "#/$defs/ServiceRequirement",
          "description": "Whether the service is required for readiness."
        },
        "service_id": {
          "description": "Stable provider-neutral service identifier.",
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/ServiceHealthStatus",
          "description": "Current observed health state."
        }
      },
      "required": [
        "service_id",
        "display_name",
        "requirement",
        "status"
      ],
      "type": "object"
    },
    "ServiceHealthStatus": {
      "description": "Health evidence reported by one provider-neutral service.",
      "oneOf": [
        {
          "const": "healthy",
          "description": "The service passed its current health check.",
          "type": "string"
        },
        {
          "const": "degraded",
          "description": "The service is available with reduced capability.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "The service cannot currently be reached or used.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "The service reported an explicit failure.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized health value.",
          "type": "string"
        }
      ]
    },
    "ServiceRequirement": {
      "description": "Whether a service is required for readiness or only improves capability.",
      "oneOf": [
        {
          "const": "mandatory",
          "description": "Readiness depends on this service.",
          "type": "string"
        },
        {
          "const": "optional",
          "description": "The platform can remain available without this service.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized requirement.",
          "type": "string"
        }
      ]
    },
    "ShutdownRequest": {
      "description": "Authenticated request for bounded sidecar shutdown.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared with the response and any safe failure."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this shutdown request."
        }
      },
      "required": [
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "ShutdownResponse": {
      "description": "Acknowledgement that bounded shutdown was accepted.",
      "properties": {
        "accepted": {
          "description": "Whether the host accepted the shutdown signal.",
          "type": "boolean"
        },
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier copied from the shutdown request."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier copied from the shutdown request."
        }
      },
      "required": [
        "accepted",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "StorageEvidence": {
      "description": "Capacity evidence for the selected privacy-preserving storage location.",
      "properties": {
        "capacity_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Total capacity in bytes."
        },
        "filesystem": {
          "$ref": "#/$defs/StringEvidence",
          "description": "Bounded filesystem name where safely available."
        },
        "free_bytes": {
          "$ref": "#/$defs/U64Evidence",
          "description": "Currently free capacity in bytes."
        },
        "location": {
          "$ref": "#/$defs/StorageLocation",
          "description": "Selected location category; no raw user path crosses the boundary."
        },
        "media_kind": {
          "$ref": "#/$defs/StorageMediaEvidence",
          "description": "Fixed, removable, or other normalized media evidence."
        }
      },
      "required": [
        "location",
        "capacity_bytes",
        "free_bytes",
        "filesystem",
        "media_kind"
      ],
      "type": "object"
    },
    "StorageLocation": {
      "description": "Privacy-preserving selected storage location category.",
      "oneOf": [
        {
          "const": "application_data",
          "description": "The per-user GixGiz application-data root.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The location is unknown or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "StorageMediaEvidence": {
      "description": "Storage-media category plus collection metadata.",
      "properties": {
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "value": {
          "anyOf": [
            {
              "$ref": "#/$defs/StorageMediaKind"
            },
            {
              "type": "null"
            }
          ],
          "description": "Normalized media kind when available."
        }
      },
      "required": [
        "metadata"
      ],
      "type": "object"
    },
    "StorageMediaKind": {
      "description": "Normalized storage-media category.",
      "oneOf": [
        {
          "const": "fixed",
          "description": "Fixed local storage.",
          "type": "string"
        },
        {
          "const": "removable",
          "description": "Removable storage.",
          "type": "string"
        },
        {
          "const": "network",
          "description": "Network-backed storage.",
          "type": "string"
        },
        {
          "const": "optical",
          "description": "Optical media.",
          "type": "string"
        },
        {
          "const": "ram_disk",
          "description": "Memory-backed storage.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "The media kind is unknown or was supplied by a newer peer.",
          "type": "string"
        }
      ]
    },
    "StringEvidence": {
      "description": "Bounded text plus its collection metadata.",
      "properties": {
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "value": {
          "description": "Normalized value when available.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "metadata"
      ],
      "type": "object"
    },
    "SupervisionNonce": {
      "description": "Opaque nonce tying a sidecar to one desktop supervision session.",
      "format": "uuid",
      "type": "string"
    },
    "TestOperationEvent": {
      "description": "Ordered provider-neutral event used to prove the Task 04 stream foundation.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by every event for the operation."
        },
        "kind": {
          "$ref": "#/$defs/TestOperationEventKind",
          "description": "Stable event classification."
        },
        "message": {
          "description": "Optional safe provider-neutral progress explanation.",
          "type": [
            "string",
            "null"
          ]
        },
        "operation_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Operation that owns this event stream."
        },
        "schema_version": {
          "description": "Schema generation for this event payload.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "sequence": {
          "description": "Monotonically increasing sequence number starting at one.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "terminal_state": {
          "anyOf": [
            {
              "$ref": "#/$defs/TestOperationTerminalState"
            },
            {
              "type": "null"
            }
          ],
          "description": "Terminal state present only on the final event."
        },
        "timestamp_unix_ms": {
          "description": "Milliseconds since the Unix epoch, captured by the host.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema_version",
        "operation_id",
        "correlation_id",
        "sequence",
        "kind",
        "timestamp_unix_ms"
      ],
      "type": "object"
    },
    "TestOperationEventKind": {
      "description": "Stable kinds emitted by the deterministic foundation event operation.",
      "oneOf": [
        {
          "const": "started",
          "description": "The operation was accepted and began.",
          "type": "string"
        },
        {
          "const": "progress",
          "description": "One deterministic progress step completed.",
          "type": "string"
        },
        {
          "const": "completed",
          "description": "The operation reached its successful terminal state.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "The operation observed explicit cancellation.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "The operation reached an explicit failed terminal state.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "The operation exceeded its bounded deadline.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an event kind this build does not recognize.",
          "type": "string"
        }
      ]
    },
    "TestOperationStartRequest": {
      "description": "Authenticated request to start the deterministic foundation event operation.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier shared by all events and safe failures for the operation."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier unique to this start request."
        }
      },
      "required": [
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "TestOperationStartResponse": {
      "description": "Accepted deterministic foundation operation.",
      "properties": {
        "correlation_id": {
          "$ref": "#/$defs/CorrelationId",
          "description": "Identifier copied from the start request."
        },
        "operation_id": {
          "$ref": "#/$defs/OperationId",
          "description": "Opaque identifier used for event subscription and cancellation."
        },
        "request_id": {
          "$ref": "#/$defs/RequestId",
          "description": "Identifier copied from the start request."
        }
      },
      "required": [
        "operation_id",
        "correlation_id",
        "request_id"
      ],
      "type": "object"
    },
    "TestOperationTerminalState": {
      "description": "Terminal result carried by the final deterministic operation event.",
      "oneOf": [
        {
          "const": "completed",
          "description": "All deterministic steps completed.",
          "type": "string"
        },
        {
          "const": "cancelled",
          "description": "Explicit cancellation was observed.",
          "type": "string"
        },
        {
          "const": "failed",
          "description": "The operation failed safely.",
          "type": "string"
        },
        {
          "const": "timed_out",
          "description": "The operation exceeded its deadline.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied a terminal state this build does not recognize.",
          "type": "string"
        }
      ]
    },
    "TransportCapability": {
      "description": "Capabilities negotiated between the desktop and the internal core sidecar.",
      "oneOf": [
        {
          "const": "health",
          "description": "Query core identity, health, and readiness.",
          "type": "string"
        },
        {
          "const": "test_operation_events",
          "description": "Start and observe the deterministic Task 04 event operation.",
          "type": "string"
        },
        {
          "const": "cancellation",
          "description": "Explicitly cancel an operation.",
          "type": "string"
        },
        {
          "const": "hardware_scan",
          "description": "Start and observe a bounded hardware evidence scan.",
          "type": "string"
        },
        {
          "const": "shutdown",
          "description": "Request bounded sidecar shutdown.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied a capability this build does not recognize.",
          "type": "string"
        }
      ]
    },
    "U32Evidence": {
      "description": "Unsigned 32-bit quantity plus its collection metadata.",
      "properties": {
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "value": {
          "description": "Normalized value when available.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "metadata"
      ],
      "type": "object"
    },
    "U64Evidence": {
      "description": "Unsigned 64-bit quantity plus its collection metadata.",
      "properties": {
        "metadata": {
          "$ref": "#/$defs/EvidenceMetadata",
          "description": "Source and availability metadata."
        },
        "value": {
          "description": "Normalized value when available.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "metadata"
      ],
      "type": "object"
    },
    "UnknownReasonCode": {
      "description": "Stable explanation category for unavailable hardware evidence.",
      "oneOf": [
        {
          "const": "not_reported",
          "description": "The provider did not report the requested field.",
          "type": "string"
        },
        {
          "const": "access_denied",
          "description": "Access was denied to the current non-elevated user.",
          "type": "string"
        },
        {
          "const": "deadline_exceeded",
          "description": "The provider exceeded the bounded collection deadline.",
          "type": "string"
        },
        {
          "const": "provider_unsupported",
          "description": "The active provider cannot collect this field reliably.",
          "type": "string"
        },
        {
          "const": "device_absent",
          "description": "The requested device or metric is not present.",
          "type": "string"
        },
        {
          "const": "source_unreliable",
          "description": "The source value is known to be unreliable for this purpose.",
          "type": "string"
        },
        {
          "const": "invalid_provider_data",
          "description": "Structured provider data failed validation or normalization.",
          "type": "string"
        },
        {
          "const": "unknown",
          "description": "A newer peer supplied an unrecognized reason.",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "bootstrap_ready": {
      "$ref": "#/$defs/BootstrapReady"
    },
    "bootstrap_request": {
      "$ref": "#/$defs/BootstrapRequest"
    },
    "cancel_operation_request": {
      "$ref": "#/$defs/CancelOperationRequest"
    },
    "cancel_operation_response": {
      "$ref": "#/$defs/CancelOperationResponse"
    },
    "client_hello": {
      "$ref": "#/$defs/ClientHello"
    },
    "core_hello": {
      "$ref": "#/$defs/CoreHello"
    },
    "hardware_scan_event": {
      "$ref": "#/$defs/HardwareScanEvent"
    },
    "hardware_scan_start_request": {
      "$ref": "#/$defs/HardwareScanStartRequest"
    },
    "hardware_scan_start_response": {
      "$ref": "#/$defs/HardwareScanStartResponse"
    },
    "health_request": {
      "$ref": "#/$defs/HealthRequest"
    },
    "health_response": {
      "$ref": "#/$defs/HealthResponse"
    },
    "safe_error_payload": {
      "$ref": "#/$defs/SafeErrorPayload"
    },
    "shutdown_request": {
      "$ref": "#/$defs/ShutdownRequest"
    },
    "shutdown_response": {
      "$ref": "#/$defs/ShutdownResponse"
    },
    "test_operation_event": {
      "$ref": "#/$defs/TestOperationEvent"
    },
    "test_operation_start_request": {
      "$ref": "#/$defs/TestOperationStartRequest"
    },
    "test_operation_start_response": {
      "$ref": "#/$defs/TestOperationStartResponse"
    }
  },
  "required": [
    "bootstrap_request",
    "bootstrap_ready",
    "client_hello",
    "core_hello",
    "health_request",
    "health_response",
    "hardware_scan_start_request",
    "hardware_scan_start_response",
    "hardware_scan_event",
    "test_operation_start_request",
    "test_operation_start_response",
    "test_operation_event",
    "cancel_operation_request",
    "cancel_operation_response",
    "shutdown_request",
    "shutdown_response",
    "safe_error_payload"
  ],
  "title": "TransportContractDocument",
  "type": "object"
}
